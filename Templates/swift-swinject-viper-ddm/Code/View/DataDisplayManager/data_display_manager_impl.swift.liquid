//
//  {{ prefix }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

class {{ prefix }}{{ module_info.name }}DataDisplayManagerImpl: NSObject {
  /// Reference to the Views's output interface.
  weak var output: {{ prefix }}{{ module_info.name }}DataDisplayManagerOutput!

  // MARK: - Other
  weak var tableView: UITableView?
}

// MARK: - CardsDataDisplayManagerInput
extension {{ prefix }}{{ module_info.name }}DataDisplayManagerImpl: {{ prefix }}{{ module_info.name }}DataDisplayManager {
  func configure(_ tableView: UITableView) {
    tableView.dataSource = self
    tableView.delegate = self

    self.tableView = tableView
  }
}

// MARK: - UITableViewDataSource
extension {{ prefix }}{{ module_info.name }}DataDisplayManagerImpl: UITableViewDataSource {
  func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    return 0
  }

  func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    let cell = UITableViewCell(style: .subtitle, reuseIdentifier: "Cell")
    return cell
  }
}

// MARK: - UITableViewDelegate
extension {{ prefix }}{{ module_info.name }}DataDisplayManagerImpl: UITableViewDelegate {
  func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    tableView.deselectRow(at: indexPath, animated: true)
  }
}
