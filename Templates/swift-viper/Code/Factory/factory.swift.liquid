//
//  {{ prefix }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

struct {{ prefix }}{{ module_info.name }}Factory {
  private init() {}

  static func make() -> UIViewController {
    return make{{ prefix }}{{ module_info.name }}ViewController()
  }
}

private extension {{ prefix }}{{ module_info.name }}Factory {
  static func make{{ prefix }}{{ module_info.name }}ViewController() -> UIViewController {
    let viewController = {{ prefix }}{{ module_info.name }}ViewController()

    let router = {{ prefix }}{{ module_info.name }}Router()
    router.view = viewController

    let interactor = {{ prefix }}{{ module_info.name }}Interactor()

    let presenter = {{ prefix }}{{ module_info.name }}Presenter()
    presenter.router = router
    presenter.view = viewController
    presenter.interactor = interactor

    interactor.output = presenter
    viewController.output = presenter

    return viewController
  }

}
